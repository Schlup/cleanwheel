CREATE TABLE person (
    id serial PRIMARY KEY,
    first_name varchar(50),
    last_name varchar(50)
);

CREATE TABLE person2 (
    id serial PRIMARY KEY,
    first_name varchar(50),
    last_name varchar(50)
);